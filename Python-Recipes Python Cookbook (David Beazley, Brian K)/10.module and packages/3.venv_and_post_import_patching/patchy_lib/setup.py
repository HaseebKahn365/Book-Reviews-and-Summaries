from setuptools import setup, find_packages

setup(
    name="patchy_lib",
    version="0.1.0",
    packages=find_packages(),
    description="A library that patches modules on import",
    author="Haseeb",
)